# Tasks

- [ ] 1.1 Write a test asserting that an item's `name`/`sku` containing markup/script constructs (for example `<img src=x onerror=alert(1)>`, or a quote followed by an event-handler attribute) is rendered as inert text in the item card's visible name, visible SKU, the quantity input's accessible name, and the quantity input's `id` attribute — REQ-CAT-10. Watch it fail against today's page.
- [ ] 1.2 Write a test asserting the same for the Order button's accessible name and its `data-sku` attribute — REQ-ORD-8. Watch it fail against today's page.
- [ ] 1.3 Write a test asserting that an item whose `sku` contains markup characters can still be ordered — and prove it through the rendered markup, not by reusing the known raw SKU as a shortcut. Parse the quantity input's `id` and the Order button's `data-sku` out of the page's actual rendered, escaped HTML (decoded the way a browser's HTML parser decodes an attribute value), drive the click-listener-attachment code path so the button's own `click` handler runs, and let it read the quantity from the input found that way and submit the order using the SKU recovered from `data-sku` — never the raw SKU held in the test's fixture. Assert the order reaches `POST /api/orders` with the SKU exactly as stored (not escaped or altered), and is accepted or rejected by the ordinary rules — REQ-CAT-10, REQ-ORD-8. A test that already knows the raw SKU and calls the order function directly does not prove the escape-on-write and decode-on-read paths agree, however its assertions read — it must go through parsing the rendered markup and the real click path. If the test harness's DOM stand-in (`test/orders.test.js`'s `loadClientPage`) does not yet support recovering `data-sku`/`id` from rendered HTML, or does not run `querySelectorAll`/`click` for real, extend it to do so rather than working around it. Watch it fail against today's page.
- [ ] 1.4 Fix the catalogue page's item-card rendering in `app/index.html` so `item.name` and `item.sku` are routed through the same `escapeHtml()` already used for the search query, everywhere the card interpolates them into `innerHTML` — the visible name, the visible SKU, the quantity input's `aria-label`, the quantity input's `id`, the Order button's `aria-label`, and the Order button's `data-sku`.
- [ ] 1.5 Run `npm run verify` and confirm the new tests pass and coverage is green.
