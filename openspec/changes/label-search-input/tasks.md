# Tasks

- [x] 1.1 Write a test asserting `GET /` serves a search field (`#q`) with an accessible name independent of its placeholder (an associated `<label for="q">` or an `aria-label`) — REQ-CAT-5. Watch it fail against today's markup.
- [x] 1.2 Add the accessible name to the search field in `app/index.html`, keeping the existing placeholder hint and search behaviour unchanged.
- [x] 1.3 Run `npm run verify` and confirm the new test passes and coverage is green.
