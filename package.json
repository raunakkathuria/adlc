{
  "name": "adlc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A working agentic delivery line: the spec is the source of truth, agents carry the work, humans hold two gates.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node app/server.mjs",
    "test": "node --test 'test/*.test.js'",
    "req-coverage": "node scripts/req-coverage.mjs",
    "verify": "npm test --silent && npm run req-coverage --silent",
    "req-ids": "node scripts/req-ids.mjs",
    "lint-workflows": "node scripts/lint-workflows.mjs"
  }
}
